---
phase: 05-react-frontend
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/LandingPage.tsx
  - frontend/src/pages/TariffsPage.tsx
  - frontend/src/api/content.ts
  - frontend/src/api/tariffs.ts
  - frontend/src/api/payments.ts
  - frontend/src/hooks/useContentBlocks.ts
  - frontend/src/hooks/useTariffs.ts
  - frontend/src/components/layout/Header.tsx
  - frontend/src/components/layout/Footer.tsx
  - frontend/src/components/landing/HeroSection.tsx
  - frontend/src/components/landing/MethodSection.tsx
  - frontend/src/components/landing/TariffsSection.tsx
  - frontend/src/components/landing/CasesSection.tsx
  - frontend/src/components/landing/FaqSection.tsx
  - frontend/src/components/landing/CtaFooter.tsx
  - frontend/src/components/payment/openPaymentWidget.ts
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Card.tsx
autonomous: true
requirements:
  - WEB-02
  - WEB-03
  - WEB-07
  - PAY-01

must_haves:
  truths:
    - "Лендинг показывает секции Hero, Метод, Тарифы, Кейсы, FAQ в правильном порядке"
    - "Тексты секций загружаются из ContentBlock API с fallback при недоступности"
    - "Карточки тарифов отображают цены из GET /api/v1/payments/tariffs/"
    - "Кнопка Оплатить открывает CloudPayments Widget с правильными параметрами"
    - "Лендинг адаптивен на 375px, 768px и 1280px"
    - "Навигация — fixed header с якорными ссылками"
  artifacts:
    - path: "frontend/src/pages/LandingPage.tsx"
      provides: "Single-page scroll лендинг"
      min_lines: 30
    - path: "frontend/src/components/landing/TariffsSection.tsx"
      provides: "Карточки тарифов из API"
      min_lines: 30
    - path: "frontend/src/components/payment/openPaymentWidget.ts"
      provides: "Обёртка CloudPayments Widget"
    - path: "frontend/src/hooks/useContentBlocks.ts"
      provides: "TanStack Query hook для ContentBlock"
  key_links:
    - from: "frontend/src/hooks/useContentBlocks.ts"
      to: "/api/v1/content/"
      via: "axios GET"
      pattern: "api.get.*content"
    - from: "frontend/src/hooks/useTariffs.ts"
      to: "/api/v1/payments/tariffs/"
      via: "axios GET"
      pattern: "api.get.*tariffs"
    - from: "frontend/src/components/payment/openPaymentWidget.ts"
      to: "window.cp.CloudPayments"
      via: "new window.cp.CloudPayments().pay()"
      pattern: "window\\.cp"
---

<objective>
Построить лендинг (single-page scroll с секциями Hero/Метод/Тарифы/Кейсы/FAQ) и страницу тарифов с интеграцией CloudPayments Widget. Тексты из ContentBlock API, цены из Tariffs API. Mobile-first вёрстка.

Purpose: Лендинг — точка входа клиента из браузера. Страница тарифов — точка оплаты для клиентов из Telegram (deep-link на /tariffs).
Output: Полностью функциональный лендинг и страница тарифов с реальными данными из API.
</objective>

<execution_context>
@/Users/a1111/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a1111/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-react-frontend/05-CONTEXT.md
@.planning/phases/05-react-frontend/05-RESEARCH.md
@.planning/phases/05-react-frontend/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 scaffold — executor should use these directly -->

From frontend/src/api/axios.ts:
```typescript
import api from '../api/axios'
// api is an axios instance with baseURL '/api/v1', JWT interceptor, and auto-refresh
```

From frontend/src/types/api.ts:
```typescript
export interface Tariff {
  id: number; code: string; title: string; price_kzt: string; description: string;
}
export interface ContentBlocks { [key: string]: string }
```

From frontend/src/types/cloudpayments.d.ts:
```typescript
// window.cp.CloudPayments available globally
// CloudPaymentsOptions: { publicId, description, amount, currency, invoiceId, accountId, data? }
// CloudPaymentsCallbacks: { onSuccess?, onFail? }
```

From frontend/src/store/authStore.ts:
```typescript
export const useAuthStore = create<AuthState>(...)
// { isAuthenticated, clientProfile, setAuth, clearAuth }
```

Backend API contracts:
- GET /api/v1/content/ → { "hero_title": "...", "hero_subtitle": "...", ... }
- GET /api/v1/payments/tariffs/ → [{ id, code, title, price_kzt, description }, ...]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: API-функции, hooks и переиспользуемые UI-компоненты</name>
  <files>
    frontend/src/api/content.ts,
    frontend/src/api/tariffs.ts,
    frontend/src/api/payments.ts,
    frontend/src/hooks/useContentBlocks.ts,
    frontend/src/hooks/useTariffs.ts,
    frontend/src/components/payment/openPaymentWidget.ts,
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Card.tsx
  </files>
  <read_first>
    frontend/src/api/axios.ts,
    frontend/src/types/api.ts,
    frontend/src/types/cloudpayments.d.ts,
    frontend/src/store/authStore.ts
  </read_first>
  <action>
    1. Создать `frontend/src/api/content.ts`:
       ```typescript
       import api from './axios'
       import type { ContentBlocks } from '../types/api'

       export async function getContentBlocks(): Promise<ContentBlocks> {
         const { data } = await api.get<ContentBlocks>('/content/')
         return data
       }
       ```

    2. Создать `frontend/src/api/tariffs.ts`:
       ```typescript
       import api from './axios'
       import type { Tariff } from '../types/api'

       export async function getTariffs(): Promise<Tariff[]> {
         const { data } = await api.get<Tariff[]>('/payments/tariffs/')
         return data
       }
       ```

    3. Создать `frontend/src/api/payments.ts`:
       ```typescript
       import api from './axios'
       import type { UpsellConfig } from '../types/api'

       export async function initiateUpsell(submissionId: string): Promise<UpsellConfig> {
         const { data } = await api.post<UpsellConfig>('/payments/upsell/', { submission_id: submissionId })
         return data
       }
       ```

    4. Создать `frontend/src/hooks/useContentBlocks.ts`:
       ```typescript
       import { useQuery } from '@tanstack/react-query'
       import { getContentBlocks } from '../api/content'

       const FALLBACK: Record<string, string> = {
         hero_title: 'Baqsy System',
         hero_subtitle: 'Профессиональный аудит вашего бизнеса',
         hero_cta: 'Выбрать тариф',
         method_title: 'Наш метод',
         method_text: 'Мы анализируем ключевые параметры вашего бизнеса и предоставляем детальный отчёт с рекомендациями по оптимизации.',
         tariff_section_title: 'Тарифы',
         cases_title: 'Кейсы клиентов',
         case_1_title: 'Ритейл-компания',
         case_1_text: 'Оптимизация бизнес-процессов привела к росту маржинальности на 15%.',
         case_2_title: 'IT-стартап',
         case_2_text: 'Аудит помог выявить узкие места и масштабировать команду.',
         faq_1_q: 'Сколько времени занимает аудит?',
         faq_1_a: 'От получения анкеты до отчёта — 3-5 рабочих дней.',
         faq_2_q: 'Какие данные нужны для аудита?',
         faq_2_a: 'Вы заполняете анкету из 27 вопросов в Telegram-боте. Дополнительных документов не требуется.',
         faq_3_q: 'Можно ли обновить тариф?',
         faq_3_a: 'Да, вы можете перейти с Ashide 1 на Ashide 2 в личном кабинете с доплатой.',
       }

       export function useContentBlocks() {
         return useQuery({
           queryKey: ['content-blocks'],
           queryFn: getContentBlocks,
           staleTime: 5 * 60 * 1000,
           placeholderData: FALLBACK,
         })
       }
       ```

    5. Создать `frontend/src/hooks/useTariffs.ts`:
       ```typescript
       import { useQuery } from '@tanstack/react-query'
       import { getTariffs } from '../api/tariffs'

       export function useTariffs() {
         return useQuery({
           queryKey: ['tariffs'],
           queryFn: getTariffs,
           staleTime: 5 * 60 * 1000,
         })
       }
       ```

    6. Создать `frontend/src/components/payment/openPaymentWidget.ts`:
       ```typescript
       interface PaymentOptions {
         publicId: string
         amount: number
         currency: string
         invoiceId: string
         description: string
         accountId: string
         data?: Record<string, unknown>
       }

       export function openPaymentWidget(
         options: PaymentOptions,
         onSuccess: () => void,
         onFail?: (reason: string) => void,
       ): void {
         if (!window.cp?.CloudPayments) {
           alert('Платёжный модуль не загружен. Обновите страницу.')
           return
         }
         const widget = new window.cp.CloudPayments()
         widget.pay('charge', options, {
           onSuccess: () => onSuccess(),
           onFail: (reason) => onFail?.(reason),
         })
       }
       ```

    7. Создать `frontend/src/components/ui/Button.tsx`:
       ```typescript
       import type { ButtonHTMLAttributes } from 'react'

       interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
         variant?: 'primary' | 'secondary' | 'outline'
         size?: 'sm' | 'md' | 'lg'
       }

       const variants = {
         primary: 'bg-slate-900 text-white hover:bg-slate-800',
         secondary: 'bg-amber-500 text-white hover:bg-amber-600',
         outline: 'border-2 border-slate-900 text-slate-900 hover:bg-slate-50',
       }

       const sizes = {
         sm: 'px-4 py-2 text-sm',
         md: 'px-6 py-3 text-base',
         lg: 'px-8 py-4 text-lg',
       }

       export function Button({ variant = 'primary', size = 'md', className = '', ...props }: ButtonProps) {
         return (
           <button
             className={`font-semibold rounded-lg transition-colors duration-200 cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed ${variants[variant]} ${sizes[size]} ${className}`}
             {...props}
           />
         )
       }
       ```

    8. Создать `frontend/src/components/ui/Card.tsx`:
       ```typescript
       interface CardProps {
         children: React.ReactNode
         className?: string
         highlight?: boolean
       }

       export function Card({ children, className = '', highlight = false }: CardProps) {
         return (
           <div className={`rounded-2xl p-6 md:p-8 ${highlight ? 'bg-slate-900 text-white shadow-2xl ring-2 ring-amber-500' : 'bg-white shadow-lg border border-slate-200'} ${className}`}>
             {children}
           </div>
         )
       }
       ```
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project/frontend" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/content.ts contains `getContentBlocks`
    - frontend/src/api/tariffs.ts contains `getTariffs`
    - frontend/src/api/payments.ts contains `initiateUpsell`
    - frontend/src/hooks/useContentBlocks.ts contains `useQuery`
    - frontend/src/hooks/useContentBlocks.ts contains `staleTime: 5 * 60 * 1000`
    - frontend/src/hooks/useContentBlocks.ts contains `placeholderData`
    - frontend/src/hooks/useContentBlocks.ts contains `hero_title`
    - frontend/src/hooks/useTariffs.ts contains `useQuery`
    - frontend/src/components/payment/openPaymentWidget.ts contains `window.cp?.CloudPayments`
    - frontend/src/components/payment/openPaymentWidget.ts contains `widget.pay('charge'`
    - frontend/src/components/ui/Button.tsx contains `export function Button`
    - frontend/src/components/ui/Card.tsx contains `export function Card`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Все API-функции, hooks и UI-примитивы созданы. TypeScript компилируется без ошибок. ContentBlocks hook использует TanStack Query с staleTime 5 минут и fallback-данными. CloudPayments Widget обёртка проверяет доступность window.cp.</done>
</task>

<task type="auto">
  <name>Task 2: Лендинг (секции + Header) и TariffsPage с CloudPayments Widget</name>
  <files>
    frontend/src/pages/LandingPage.tsx,
    frontend/src/pages/TariffsPage.tsx,
    frontend/src/components/layout/Header.tsx,
    frontend/src/components/layout/Footer.tsx,
    frontend/src/components/landing/HeroSection.tsx,
    frontend/src/components/landing/MethodSection.tsx,
    frontend/src/components/landing/TariffsSection.tsx,
    frontend/src/components/landing/CasesSection.tsx,
    frontend/src/components/landing/FaqSection.tsx,
    frontend/src/components/landing/CtaFooter.tsx
  </files>
  <read_first>
    frontend/src/hooks/useContentBlocks.ts,
    frontend/src/hooks/useTariffs.ts,
    frontend/src/components/payment/openPaymentWidget.ts,
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Card.tsx,
    frontend/src/types/api.ts,
    frontend/src/store/authStore.ts,
    .planning/phases/05-react-frontend/05-CONTEXT.md
  </read_first>
  <action>
    Стиль: «солидный/премиальный», тёмные акценты. Цвета: slate-900, slate-800, amber-500 для акцентов, white/slate-50 для фона. Mobile-first Tailwind (sm/md/lg breakpoints).

    1. `frontend/src/components/layout/Header.tsx`:
       - Fixed header: `fixed top-0 w-full z-50 bg-white/95 backdrop-blur border-b border-slate-200`
       - Лого «Baqsy System» слева (text-xl font-bold text-slate-900)
       - Якорные ссылки: Метод, Тарифы, Кейсы, FAQ (скрыты на mobile, видны на md+)
       - Кнопка «Выбрать тариф» справа (link на #tariffs)
       - Мобильное меню: hamburger button (md:hidden), показывает/скрывает список ссылок

    2. `frontend/src/components/layout/Footer.tsx`:
       - Тёмный фон (bg-slate-900 text-white)
       - Копирайт: `(c) {new Date().getFullYear()} Baqsy System`
       - Контактная информация заглушка

    3. `frontend/src/components/landing/HeroSection.tsx`:
       - Props: `content: Record<string, string>`
       - Полноэкранная секция (min-h-screen flex items-center)
       - `content.hero_title` как h1 (text-4xl md:text-6xl font-bold)
       - `content.hero_subtitle` как p (text-lg md:text-xl text-slate-600)
       - Кнопка CTA `content.hero_cta` → scroll к #tariffs
       - Градиентный или solid тёмный фон опционально

    4. `frontend/src/components/landing/MethodSection.tsx`:
       - Props: `content: Record<string, string>`
       - id="method"
       - `content.method_title` как h2
       - `content.method_text` как параграф
       - Простая секция с иконками или числовыми шагами (1, 2, 3)

    5. `frontend/src/components/landing/TariffsSection.tsx`:
       - Props: `content: Record<string, string>`, использует `useTariffs()` hook
       - id="tariffs"
       - Заголовок из `content.tariff_section_title`
       - Две карточки (Card) тарифов из API:
         - Показывают title, price_kzt (форматировать с пробелами: `45 000 ₸`), description
         - Ashide 2 карточка с `highlight={true}` (рекомендованный)
         - Кнопка «Оплатить» на каждой карточке
       - При клике «Оплатить»:
         - Проверить `useAuthStore.getState().isAuthenticated`
         - Если не залогинен: показать alert/toast «Начните с Telegram-бота: @baqsy_bot для оплаты»
         - Если залогинен: вызвать `openPaymentWidget()` с `publicId` из `import.meta.env.VITE_CLOUDPAYMENTS_PUBLIC_ID`, amount из тарифа, currency "KZT", invoiceId — нужен submission UUID (получить из submission query или показать сообщение)
       - Функция форматирования цены: `function formatPrice(price: string): string { return Number(price).toLocaleString('ru-RU') + ' ₸' }`
       - Состояние loading: skeleton карточек

    6. `frontend/src/components/landing/CasesSection.tsx`:
       - Props: `content: Record<string, string>`
       - id="cases"
       - `content.cases_title` как h2
       - Карточки кейсов: `case_1_title`/`case_1_text`, `case_2_title`/`case_2_text`
       - Grid: 1 col на mobile, 2 cols на md+

    7. `frontend/src/components/landing/FaqSection.tsx`:
       - Props: `content: Record<string, string>`
       - id="faq"
       - Accordion-стиль FAQ: каждый `faq_N_q` + `faq_N_a` пара
       - Динамически находить все ключи `faq_*_q` из content
       - useState для открытого/закрытого состояния каждого вопроса
       - Анимация: `max-h-0 overflow-hidden transition-all` → `max-h-96` при открытии

    8. `frontend/src/components/landing/CtaFooter.tsx`:
       - Финальный CTA блок перед Footer
       - «Готовы к аудиту?» + кнопка на Telegram бот

    9. `frontend/src/pages/LandingPage.tsx`:
       - Использует `useContentBlocks()` hook
       - Рендерит в порядке: Header → HeroSection → MethodSection → TariffsSection → CasesSection → FaqSection → CtaFooter → Footer
       - Передаёт `content` (data из hook) каждому компоненту
       - `pt-16` или `pt-20` для offset от fixed header
       - `scroll-smooth` на контейнере

    10. `frontend/src/pages/TariffsPage.tsx`:
        - Отдельная страница для deep-link из бота (маршрут /tariffs)
        - Использует `useTariffs()` hook
        - Рендерит Header + карточки тарифов + кнопки оплаты (аналогично TariffsSection, но как полная страница)
        - Если клиент залогинен (JWT есть): загрузить его submission через `GET /api/v1/submissions/my/`, использовать submission.id как invoiceId для CP Widget
        - Если не залогинен: показать «Для оплаты начните с Telegram-бота» с ссылкой
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project/frontend" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/LandingPage.tsx contains `useContentBlocks`
    - frontend/src/pages/LandingPage.tsx contains `HeroSection`
    - frontend/src/pages/LandingPage.tsx contains `TariffsSection`
    - frontend/src/pages/LandingPage.tsx contains `FaqSection`
    - frontend/src/components/landing/HeroSection.tsx contains `hero_title`
    - frontend/src/components/landing/TariffsSection.tsx contains `useTariffs`
    - frontend/src/components/landing/TariffsSection.tsx contains `openPaymentWidget`
    - frontend/src/components/landing/TariffsSection.tsx contains `formatPrice`
    - frontend/src/components/landing/FaqSection.tsx contains `faq_`
    - frontend/src/components/layout/Header.tsx contains `fixed`
    - frontend/src/components/layout/Header.tsx contains `md:hidden` or `md:flex`
    - frontend/src/pages/TariffsPage.tsx contains `useTariffs`
    - frontend/src/pages/TariffsPage.tsx contains `submissions/my`
    - npm run build exits 0
  </acceptance_criteria>
  <done>Лендинг отображает все 6 секций с текстами из ContentBlock API (fallback при недоступности). Карточки тарифов показывают цены из API. Кнопка оплаты вызывает CloudPayments Widget. Страница /tariffs работает как отдельная точка входа. Вёрстка адаптивна (mobile-first).</done>
</task>

</tasks>

<verification>
- `npm run build` проходит без ошибок
- Все компоненты лендинга используют данные из hooks (не захардкоженные)
- TariffsSection загружает цены из API
- CloudPayments Widget вызывается через openPaymentWidget, не напрямую
- Header фиксирован и адаптивен
</verification>

<success_criteria>
- Лендинг показывает Hero, Метод, Тарифы, Кейсы, FAQ (WEB-02)
- Тексты из ContentBlock API с fallback (WEB-02)
- Карточки тарифов с ценами из API и кнопкой оплаты через CP Widget (WEB-03, PAY-01)
- Mobile-first адаптивная вёрстка (WEB-07)
- npm run build проходит
</success_criteria>

<output>
After completion, create `.planning/phases/05-react-frontend/05-02-SUMMARY.md`
</output>
