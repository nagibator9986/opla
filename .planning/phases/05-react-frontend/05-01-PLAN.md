---
phase: 05-react-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/apps/content/views.py
  - backend/apps/content/serializers.py
  - backend/apps/content/urls.py
  - backend/apps/content/tests/test_views.py
  - backend/apps/core/api_urls.py
  - backend/apps/submissions/serializers.py
  - backend/apps/submissions/views.py
  - backend/apps/submissions/urls.py
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/api/axios.ts
  - frontend/src/store/authStore.ts
  - frontend/src/types/api.ts
  - frontend/src/types/cloudpayments.d.ts
  - frontend/src/router/index.tsx
  - frontend/src/router/ProtectedRoute.tsx
  - frontend/Dockerfile
  - docker/docker-compose.yml
  - docker/nginx.conf
  - .env.example
autonomous: true
requirements:
  - WEB-01
  - WEB-06
  - WEB-08

must_haves:
  truths:
    - "npm run build в frontend/ завершается без ошибок"
    - "GET /api/v1/content/ возвращает JSON-словарь активных ContentBlock"
    - "GET /api/v1/submissions/{id}/ возвращает поля tariff_code и pdf_url"
    - "GET /api/v1/submissions/my/ возвращает последний submission JWT-клиента"
    - "frontend-сервис стартует в docker-compose и отвечает на 5173"
    - "Vite proxy /api перенаправляет запросы на Django"
  artifacts:
    - path: "backend/apps/content/views.py"
      provides: "ContentBlock list API"
      exports: ["ContentBlockListView"]
    - path: "frontend/src/api/axios.ts"
      provides: "Axios instance с JWT interceptor и авто-рефрешем"
    - path: "frontend/src/store/authStore.ts"
      provides: "Zustand auth store"
    - path: "frontend/src/router/index.tsx"
      provides: "React Router с 4 маршрутами"
  key_links:
    - from: "frontend/src/api/axios.ts"
      to: "/api/v1/"
      via: "Vite proxy в vite.config.ts"
      pattern: "proxy.*api.*web:8000"
    - from: "backend/apps/content/urls.py"
      to: "backend/apps/core/api_urls.py"
      via: "include('apps.content.urls')"
      pattern: "content/"
---

<objective>
Подготовить фундамент для React-фронтенда: создать недостающий backend API (ContentBlock, расширить SubmissionDetailSerializer), развернуть Vite-проект со всеми зависимостями, настроить инфраструктурные слои (axios+interceptor, Zustand auth store, router, TypeScript типы), интегрировать frontend в docker-compose.

Purpose: Без этого фундамента последующие планы (лендинг, кабинет) не могут ни получить данные из API, ни запустить dev-сервер.
Output: Работающий Vite dev-сервер в docker-compose, ContentBlock API endpoint, расширенный SubmissionDetailSerializer, вся инфраструктура фронтенда (axios, Zustand, router, типы).
</objective>

<execution_context>
@/Users/a1111/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a1111/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-react-frontend/05-CONTEXT.md
@.planning/phases/05-react-frontend/05-RESEARCH.md

<interfaces>
<!-- Backend models and views the executor needs to know about -->

From backend/apps/content/models.py:
```python
class ContentBlock(TimestampedModel):
    class ContentType(models.TextChoices):
        TEXT = "text", "Текст"
        HTML = "html", "HTML"
    key = models.SlugField(max_length=100, unique=True)
    title = models.CharField(max_length=255)
    content = models.TextField(blank=True)
    content_type = models.CharField(max_length=10, choices=ContentType.choices, default=ContentType.TEXT)
    is_active = models.BooleanField(default=True)
```

From backend/apps/reports/models.py:
```python
class AuditReport(TimestampedModel):
    submission = models.OneToOneField("submissions.Submission", on_delete=models.CASCADE, related_name="report")
    pdf_url = models.URLField(blank=True)
    status = models.CharField(max_length=20, choices=Status.choices, default=Status.DRAFT)
```

From backend/apps/submissions/serializers.py:
```python
class SubmissionDetailSerializer(serializers.ModelSerializer):
    # fields: id, status, template_name, industry_name, total_questions, answered_count, created_at, completed_at
```

From backend/apps/accounts/views.py:
```python
class DeeplinkExchangeView(APIView):
    # Returns: {"access": "...", "refresh": "...", "client_profile_id": 42, "name": "Иван Петров"}
```

From backend/apps/payments/views.py:
```python
class UpsellView(APIView):
    # POST /api/v1/payments/upsell/ — requires submission_id in body
    # Returns: {"publicId": "...", "amount": 90000, "currency": "KZT", "invoiceId": "uuid", ...}
```

From backend/apps/core/api_urls.py:
```python
urlpatterns = [
    path("bot/", include("apps.accounts.bot_urls")),
    path("industries/", include("apps.industries.urls")),
    path("submissions/", include("apps.submissions.urls")),
    path("payments/", include("apps.payments.urls")),
]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend API — ContentBlock endpoint + SubmissionDetailSerializer расширение + my-submission endpoint</name>
  <files>
    backend/apps/content/views.py,
    backend/apps/content/serializers.py,
    backend/apps/content/urls.py,
    backend/apps/content/tests/test_views.py,
    backend/apps/core/api_urls.py,
    backend/apps/submissions/serializers.py,
    backend/apps/submissions/views.py,
    backend/apps/submissions/urls.py
  </files>
  <read_first>
    backend/apps/content/models.py,
    backend/apps/content/__init__.py,
    backend/apps/core/api_urls.py,
    backend/apps/submissions/serializers.py,
    backend/apps/submissions/views.py,
    backend/apps/submissions/urls.py,
    backend/apps/reports/models.py,
    backend/apps/payments/models.py
  </read_first>
  <action>
    1. Создать `backend/apps/content/serializers.py`:
       - Класс `ContentBlockSerializer(serializers.ModelSerializer)` с `fields = ["key", "content", "content_type"]`

    2. Создать `backend/apps/content/views.py`:
       - Класс `ContentBlockListView(APIView)` с `permission_classes = [AllowAny]`
       - GET handler: `ContentBlock.objects.filter(is_active=True)` → возвращает dict `{block.key: block.content for block in qs}`
       - Response — плоский JSON-словарь, не список: `{"hero_title": "...", "hero_subtitle": "...", ...}`

    3. Создать `backend/apps/content/urls.py`:
       ```python
       from django.urls import path
       from apps.content.views import ContentBlockListView
       urlpatterns = [
           path("", ContentBlockListView.as_view(), name="content-list"),
       ]
       ```

    4. Обновить `backend/apps/core/api_urls.py` — добавить:
       ```python
       path("content/", include("apps.content.urls")),
       ```

    5. Расширить `backend/apps/submissions/serializers.py` — в `SubmissionDetailSerializer`:
       - Добавить `tariff_code = serializers.CharField(source="tariff.code", read_only=True, default=None)`
       - Добавить `pdf_url = serializers.SerializerMethodField()`
       - Метод `get_pdf_url(self, obj)`: `report = getattr(obj, 'report', None); return report.pdf_url if report and report.pdf_url else None`
       - Добавить `tariff_code` и `pdf_url` в `fields` и `read_only_fields`

    6. Добавить `MySubmissionView` в `backend/apps/submissions/views.py`:
       - GET endpoint, `permission_classes = [IsAuthenticated]`
       - Находит `_get_client_profile(request.user)`, затем `Submission.objects.filter(client=client).order_by("-created_at").first()`
       - Если нет submission → 404 с `{"detail": "Нет активных заказов."}`
       - Если есть → `SubmissionDetailSerializer(sub).data`

    7. Добавить маршрут в `backend/apps/submissions/urls.py`:
       ```python
       path("my/", MySubmissionView.as_view(), name="submission-my"),
       ```
       ВАЖНО: поставить ДО `path("<uuid:pk>/", ...)` чтобы `my` не перехватывался как UUID.

    8. Создать `backend/apps/content/tests/test_views.py`:
       - `test_content_list_returns_active_blocks`: создать 2 active + 1 inactive ContentBlock, GET `/api/v1/content/`, проверить что возвращается dict с 2 ключами, inactive не включён
       - `test_content_list_empty`: проверить что пустой ответ = `{}`
       - `test_content_list_allows_anonymous`: запрос без auth проходит (status 200)
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project" && docker-compose -f docker/docker-compose.yml exec web pytest apps/content/tests/test_views.py -x -q 2>/dev/null || cd backend && python -m pytest apps/content/tests/test_views.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/apps/content/views.py contains `class ContentBlockListView`
    - backend/apps/content/views.py contains `permission_classes = [AllowAny]`
    - backend/apps/content/serializers.py contains `class ContentBlockSerializer`
    - backend/apps/content/urls.py contains `path("", ContentBlockListView`
    - backend/apps/core/api_urls.py contains `path("content/", include("apps.content.urls"))`
    - backend/apps/submissions/serializers.py contains `tariff_code`
    - backend/apps/submissions/serializers.py contains `def get_pdf_url`
    - backend/apps/submissions/serializers.py contains `report.pdf_url`
    - backend/apps/submissions/views.py contains `class MySubmissionView`
    - backend/apps/submissions/urls.py contains `path("my/", MySubmissionView`
    - backend/apps/content/tests/test_views.py contains `test_content_list_returns_active_blocks`
    - pytest apps/content/tests/test_views.py exits 0
  </acceptance_criteria>
  <done>ContentBlock API возвращает dict активных блоков на GET /api/v1/content/. SubmissionDetailSerializer включает tariff_code и pdf_url. GET /api/v1/submissions/my/ возвращает последний submission клиента. Все тесты проходят.</done>
</task>

<task type="auto">
  <name>Task 2: Vite scaffold + infrastructure (axios, Zustand, router, types, Docker)</name>
  <files>
    frontend/package.json,
    frontend/vite.config.ts,
    frontend/tsconfig.json,
    frontend/index.html,
    frontend/src/main.tsx,
    frontend/src/App.tsx,
    frontend/src/index.css,
    frontend/src/api/axios.ts,
    frontend/src/store/authStore.ts,
    frontend/src/types/api.ts,
    frontend/src/types/cloudpayments.d.ts,
    frontend/src/router/index.tsx,
    frontend/src/router/ProtectedRoute.tsx,
    frontend/src/pages/LandingPage.tsx,
    frontend/src/pages/TariffsPage.tsx,
    frontend/src/pages/AuthPage.tsx,
    frontend/src/pages/CabinetPage.tsx,
    frontend/Dockerfile,
    docker/docker-compose.yml,
    docker/nginx.conf,
    .env.example
  </files>
  <read_first>
    docker/docker-compose.yml,
    docker/nginx.conf,
    .env.example,
    .planning/phases/05-react-frontend/05-RESEARCH.md
  </read_first>
  <action>
    1. Создать Vite проект:
       ```bash
       cd "/Users/a1111/Desktop/projects/oplata project"
       npm create vite@latest frontend -- --template react-ts
       cd frontend
       npm install @tanstack/react-query @tanstack/react-query-devtools zustand react-router-dom axios tailwindcss @tailwindcss/vite
       ```

    2. Обновить `frontend/vite.config.ts`:
       ```typescript
       import { defineConfig } from 'vite'
       import react from '@vitejs/plugin-react'
       import tailwindcss from '@tailwindcss/vite'

       export default defineConfig({
         plugins: [react(), tailwindcss()],
         server: {
           host: '0.0.0.0',
           port: 5173,
           proxy: {
             '/api': { target: 'http://web:8000', changeOrigin: true },
           },
         },
       })
       ```

    3. Обновить `frontend/src/index.css`:
       ```css
       @import "tailwindcss";
       ```

    4. Обновить `frontend/index.html` — добавить перед закрывающим `</head>`:
       ```html
       <script src="https://widget.cloudpayments.ru/bundles/cloudpayments.js"></script>
       ```

    5. Создать `frontend/src/types/api.ts`:
       ```typescript
       export interface Tariff {
         id: number
         code: string
         title: string
         price_kzt: string
         description: string
       }

       export interface Submission {
         id: string
         status: string
         template_name: string
         industry_name: string
         total_questions: number
         answered_count: number
         tariff_code: string | null
         pdf_url: string | null
         created_at: string
         completed_at: string | null
       }

       export interface DeeplinkResponse {
         access: string
         refresh: string
         client_profile_id: number
         name: string
       }

       export interface UpsellConfig {
         publicId: string
         amount: number
         currency: string
         invoiceId: string
         description: string
         accountId: string
         tariff_code: string
       }

       export interface ContentBlocks {
         [key: string]: string
       }
       ```

    6. Создать `frontend/src/types/cloudpayments.d.ts`:
       ```typescript
       export {}

       declare global {
         interface Window {
           cp: {
             CloudPayments: new () => {
               pay: (
                 mode: 'charge',
                 options: CloudPaymentsOptions,
                 callbacks: CloudPaymentsCallbacks,
               ) => void
             }
           }
         }
       }

       interface CloudPaymentsOptions {
         publicId: string
         description: string
         amount: number
         currency: string
         invoiceId: string
         accountId: string
         data?: Record<string, unknown>
       }

       interface CloudPaymentsCallbacks {
         onSuccess?: (options: CloudPaymentsOptions) => void
         onFail?: (reason: string, options: CloudPaymentsOptions) => void
       }
       ```

    7. Создать `frontend/src/api/axios.ts` — axios instance с JWT interceptor и авто-рефрешем:
       - baseURL = `import.meta.env.VITE_API_URL ?? '/api/v1'`
       - Request interceptor: добавляет `Authorization: Bearer {access_token}` из localStorage
       - Response interceptor: при 401 пытается рефрешить через `POST /api/v1/auth/token/refresh/` с `refresh` из localStorage
       - При успехе рефреша: обновляет access_token в localStorage, повторяет оригинальный запрос
       - При провале рефреша: вызывает `useAuthStore.getState().clearAuth()`, reject
       - Очередь failedQueue для параллельных запросов во время рефреша
       - Флаг `isRefreshing` для предотвращения множественных рефрешей
       (Полный код есть в 05-RESEARCH.md Pattern 1)

    8. Создать `frontend/src/store/authStore.ts` — Zustand store:
       ```typescript
       import { create } from 'zustand'

       interface ClientProfile {
         id: number
         name: string
       }

       interface AuthState {
         isAuthenticated: boolean
         clientProfile: ClientProfile | null
         setAuth: (profile: ClientProfile, accessToken: string, refreshToken: string) => void
         clearAuth: () => void
       }

       export const useAuthStore = create<AuthState>((set) => ({
         isAuthenticated: !!localStorage.getItem('access_token'),
         clientProfile: JSON.parse(localStorage.getItem('client_profile') ?? 'null'),
         setAuth: (profile, accessToken, refreshToken) => {
           localStorage.setItem('access_token', accessToken)
           localStorage.setItem('refresh_token', refreshToken)
           localStorage.setItem('client_profile', JSON.stringify(profile))
           set({ isAuthenticated: true, clientProfile: profile })
         },
         clearAuth: () => {
           localStorage.removeItem('access_token')
           localStorage.removeItem('refresh_token')
           localStorage.removeItem('client_profile')
           set({ isAuthenticated: false, clientProfile: null })
         },
       }))
       ```

    9. Создать `frontend/src/router/ProtectedRoute.tsx`:
       ```typescript
       import { Navigate } from 'react-router-dom'
       import { useAuthStore } from '../store/authStore'

       export function ProtectedRoute({ children }: { children: React.ReactNode }) {
         const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
         if (!isAuthenticated) return <Navigate to="/" replace />
         return <>{children}</>
       }
       ```

    10. Создать `frontend/src/router/index.tsx` — 4 маршрута:
        ```typescript
        import { createBrowserRouter } from 'react-router-dom'
        import { LandingPage } from '../pages/LandingPage'
        import { TariffsPage } from '../pages/TariffsPage'
        import { AuthPage } from '../pages/AuthPage'
        import { CabinetPage } from '../pages/CabinetPage'
        import { ProtectedRoute } from './ProtectedRoute'

        export const router = createBrowserRouter([
          { path: '/', element: <LandingPage /> },
          { path: '/tariffs', element: <TariffsPage /> },
          { path: '/auth/:uuid', element: <AuthPage /> },
          { path: '/cabinet', element: <ProtectedRoute><CabinetPage /></ProtectedRoute> },
        ])
        ```

    11. Создать 4 страницы-заглушки (каждая экспортирует именованную функцию):
        - `frontend/src/pages/LandingPage.tsx` — `export function LandingPage() { return <div>Landing</div> }`
        - `frontend/src/pages/TariffsPage.tsx` — `export function TariffsPage() { return <div>Tariffs</div> }`
        - `frontend/src/pages/AuthPage.tsx` — `export function AuthPage() { return <div>Auth</div> }`
        - `frontend/src/pages/CabinetPage.tsx` — `export function CabinetPage() { return <div>Cabinet</div> }`

    12. Обновить `frontend/src/main.tsx`:
        ```typescript
        import { StrictMode } from 'react'
        import { createRoot } from 'react-dom/client'
        import { QueryClient, QueryClientProvider } from '@tanstack/react-query'
        import { RouterProvider } from 'react-router-dom'
        import { router } from './router'
        import './index.css'

        const queryClient = new QueryClient({
          defaultOptions: { queries: { retry: 1, staleTime: 30_000 } },
        })

        createRoot(document.getElementById('root')!).render(
          <StrictMode>
            <QueryClientProvider client={queryClient}>
              <RouterProvider router={router} />
            </QueryClientProvider>
          </StrictMode>,
        )
        ```

    13. Удалить `frontend/src/App.tsx` и `frontend/src/App.css` (не нужны при RouterProvider).

    14. Создать `frontend/Dockerfile`:
        ```dockerfile
        FROM node:22-alpine
        WORKDIR /app
        COPY package*.json ./
        RUN npm install
        COPY . .
        EXPOSE 5173
        CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
        ```

    15. Обновить `docker/docker-compose.yml` — добавить сервис `frontend` перед `nginx`:
        ```yaml
        frontend:
          build:
            context: ../frontend
            dockerfile: Dockerfile
          volumes:
            - ../frontend:/app
            - /app/node_modules
          ports:
            - "5173:5173"
          command: npm run dev -- --host 0.0.0.0
          environment:
            - VITE_API_URL=/api/v1
            - VITE_CLOUDPAYMENTS_PUBLIC_ID=${VITE_CLOUDPAYMENTS_PUBLIC_ID:-test_api_00000000000000000000001}
        ```

    16. Обновить `docker/nginx.conf` — добавить location для фронта (перед `location /`):
        ```nginx
        # Frontend dev proxy
        location /assets/ {
            proxy_pass http://frontend:5173;
        }

        location /@vite/ {
            proxy_pass http://frontend:5173;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location /api/ {
            proxy_pass http://django;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location /admin/ {
            proxy_pass http://django;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # SPA — fallback to Vite dev server for all other routes
        location / {
            proxy_pass http://frontend:5173;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
        }
        ```

    17. Обновить `.env.example` — добавить в конец:
        ```
        # Frontend (Phase 5)
        VITE_CLOUDPAYMENTS_PUBLIC_ID=
        VITE_API_URL=/api/v1
        ```
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project/frontend" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "@tanstack/react-query"
    - frontend/package.json contains "zustand"
    - frontend/package.json contains "react-router-dom"
    - frontend/package.json contains "axios"
    - frontend/package.json contains "tailwindcss"
    - frontend/package.json contains "@tailwindcss/vite"
    - frontend/vite.config.ts contains "proxy"
    - frontend/vite.config.ts contains "web:8000"
    - frontend/vite.config.ts contains "tailwindcss()"
    - frontend/index.html contains "cloudpayments.js"
    - frontend/src/index.css contains '@import "tailwindcss"'
    - frontend/src/api/axios.ts contains "interceptors.response.use"
    - frontend/src/api/axios.ts contains "token/refresh"
    - frontend/src/api/axios.ts contains "clearAuth"
    - frontend/src/store/authStore.ts contains "create<AuthState>"
    - frontend/src/store/authStore.ts contains "localStorage.setItem"
    - frontend/src/router/index.tsx contains "createBrowserRouter"
    - frontend/src/router/index.tsx contains "/auth/:uuid"
    - frontend/src/router/index.tsx contains "/cabinet"
    - frontend/src/router/ProtectedRoute.tsx contains "Navigate"
    - frontend/src/types/api.ts contains "interface Tariff"
    - frontend/src/types/api.ts contains "interface Submission"
    - frontend/src/types/cloudpayments.d.ts contains "CloudPayments"
    - frontend/src/main.tsx contains "QueryClientProvider"
    - frontend/src/main.tsx contains "RouterProvider"
    - frontend/Dockerfile contains "node:22-alpine"
    - docker/docker-compose.yml contains "frontend"
    - docker/docker-compose.yml contains "VITE_CLOUDPAYMENTS_PUBLIC_ID"
    - docker/nginx.conf contains "frontend:5173"
    - .env.example contains "VITE_CLOUDPAYMENTS_PUBLIC_ID"
    - npm run build exits 0
  </acceptance_criteria>
  <done>Vite-проект собирается без ошибок. Все зависимости установлены. Axios interceptor, Zustand store, router с 4 маршрутами, TypeScript типы, CloudPayments type declarations на месте. Docker-compose включает frontend-сервис. Nginx проксирует фронт и API.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` проходит без ошибок
- `pytest apps/content/tests/test_views.py -x` все тесты зелёные
- `docker-compose config` не выдаёт ошибок (валидный YAML с frontend-сервисом)
- `grep -r "content/" backend/apps/core/api_urls.py` находит маршрут
</verification>

<success_criteria>
- Backend API для ContentBlock доступен на GET /api/v1/content/
- SubmissionDetailSerializer возвращает tariff_code и pdf_url
- GET /api/v1/submissions/my/ возвращает последний submission клиента
- frontend/npm run build завершается без ошибок (WEB-01)
- Все инфраструктурные файлы на месте: axios, store, router, types (WEB-08)
- Docker-compose включает frontend-сервис
</success_criteria>

<output>
After completion, create `.planning/phases/05-react-frontend/05-01-SUMMARY.md`
</output>
