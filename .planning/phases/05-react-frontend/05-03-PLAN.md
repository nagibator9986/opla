---
phase: 05-react-frontend
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/pages/AuthPage.tsx
  - frontend/src/pages/CabinetPage.tsx
  - frontend/src/api/auth.ts
  - frontend/src/api/submissions.ts
  - frontend/src/hooks/useSubmission.ts
  - frontend/src/components/cabinet/StatusProgress.tsx
  - frontend/src/components/cabinet/PdfDownloadButton.tsx
  - frontend/src/components/cabinet/UpsellCard.tsx
autonomous: true
requirements:
  - WEB-04
  - WEB-05
  - WEB-06

must_haves:
  truths:
    - "Клиент переходит по /auth/:uuid → React обменивает UUID на JWT → редирект в /cabinet"
    - "При невалидном/истёкшем UUID показывается сообщение об ошибке"
    - "Кабинет показывает step progress bar со статусом заказа"
    - "Кнопка Upsell видна только клиентам с tariff_code ashide_1"
    - "При status=delivered доступна кнопка Скачать PDF"
    - "Защищённый маршрут /cabinet редиректит на / без JWT"
  artifacts:
    - path: "frontend/src/pages/AuthPage.tsx"
      provides: "Deep-link exchange: UUID → JWT → redirect"
      min_lines: 25
    - path: "frontend/src/pages/CabinetPage.tsx"
      provides: "Личный кабинет со статусом и upsell"
      min_lines: 40
    - path: "frontend/src/components/cabinet/StatusProgress.tsx"
      provides: "Step progress bar"
    - path: "frontend/src/components/cabinet/UpsellCard.tsx"
      provides: "Кнопка upsell с CP Widget"
  key_links:
    - from: "frontend/src/pages/AuthPage.tsx"
      to: "/api/v1/bot/deeplink/exchange/"
      via: "axios POST"
      pattern: "deeplink/exchange"
    - from: "frontend/src/pages/CabinetPage.tsx"
      to: "/api/v1/submissions/my/"
      via: "useSubmission hook"
      pattern: "submissions/my"
    - from: "frontend/src/components/cabinet/UpsellCard.tsx"
      to: "/api/v1/payments/upsell/"
      via: "initiateUpsell"
      pattern: "payments/upsell"
---

<objective>
Реализовать deep-link авторизацию (UUID → JWT) и личный кабинет клиента со статусом заказа, кнопкой скачивания PDF и upsell. Клиент приходит из Telegram-бота по ссылке /auth/{uuid}, автоматически логинится и видит свой заказ.

Purpose: Кабинет — единственное место, где клиент отслеживает прогресс аудита и скачивает PDF. Deep-link — единственный способ авторизации на сайте (без email/пароля).
Output: Работающий auth flow и кабинет с upsell.
</objective>

<execution_context>
@/Users/a1111/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a1111/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-react-frontend/05-CONTEXT.md
@.planning/phases/05-react-frontend/05-RESEARCH.md
@.planning/phases/05-react-frontend/05-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — executor should use these directly -->

From frontend/src/api/axios.ts:
```typescript
import api from '../api/axios'
// axios instance with baseURL '/api/v1', JWT interceptor, auto-refresh on 401
```

From frontend/src/store/authStore.ts:
```typescript
export const useAuthStore = create<AuthState>((set) => ({
  isAuthenticated: boolean,
  clientProfile: { id: number, name: string } | null,
  setAuth: (profile, accessToken, refreshToken) => void,
  clearAuth: () => void,
}))
```

From frontend/src/types/api.ts:
```typescript
export interface Submission {
  id: string; status: string; template_name: string; industry_name: string;
  total_questions: number; answered_count: number; tariff_code: string | null;
  pdf_url: string | null; created_at: string; completed_at: string | null;
}
export interface DeeplinkResponse {
  access: string; refresh: string; client_profile_id: number; name: string;
}
export interface UpsellConfig {
  publicId: string; amount: number; currency: string; invoiceId: string;
  description: string; accountId: string; tariff_code: string;
}
```

From frontend/src/components/payment/openPaymentWidget.ts:
```typescript
export function openPaymentWidget(options, onSuccess, onFail?): void
```

Backend API contracts:
- POST /api/v1/bot/deeplink/exchange/ { token: "uuid" } → { access, refresh, client_profile_id, name }
- GET /api/v1/submissions/my/ → Submission object (tariff_code, pdf_url included)
- POST /api/v1/payments/upsell/ { submission_id: "uuid" } → UpsellConfig
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auth flow (deep-link exchange) + API/hooks для submissions</name>
  <files>
    frontend/src/pages/AuthPage.tsx,
    frontend/src/api/auth.ts,
    frontend/src/api/submissions.ts,
    frontend/src/hooks/useSubmission.ts
  </files>
  <read_first>
    frontend/src/api/axios.ts,
    frontend/src/store/authStore.ts,
    frontend/src/types/api.ts,
    frontend/src/router/index.tsx,
    frontend/src/router/ProtectedRoute.tsx
  </read_first>
  <action>
    1. Создать `frontend/src/api/auth.ts`:
       ```typescript
       import axios from 'axios'
       import type { DeeplinkResponse } from '../types/api'

       const baseURL = import.meta.env.VITE_API_URL ?? '/api/v1'

       export async function exchangeDeeplink(uuid: string): Promise<DeeplinkResponse> {
         const { data } = await axios.post<DeeplinkResponse>(`${baseURL}/bot/deeplink/exchange/`, { token: uuid })
         return data
       }
       ```
       ВАЖНО: использовать plain axios (не api instance), потому что у клиента ещё нет JWT в момент exchange. Не нужен interceptor.

    2. Создать `frontend/src/api/submissions.ts`:
       ```typescript
       import api from './axios'
       import type { Submission } from '../types/api'

       export async function getMySubmission(): Promise<Submission> {
         const { data } = await api.get<Submission>('/submissions/my/')
         return data
       }
       ```

    3. Создать `frontend/src/hooks/useSubmission.ts`:
       ```typescript
       import { useQuery } from '@tanstack/react-query'
       import { getMySubmission } from '../api/submissions'
       import { useAuthStore } from '../store/authStore'

       export function useSubmission() {
         const isAuthenticated = useAuthStore((s) => s.isAuthenticated)
         return useQuery({
           queryKey: ['my-submission'],
           queryFn: getMySubmission,
           enabled: isAuthenticated,
           staleTime: 30_000,
         })
       }
       ```

    4. Реализовать `frontend/src/pages/AuthPage.tsx` (заменить заглушку):
       ```typescript
       import { useEffect, useState } from 'react'
       import { useNavigate, useParams } from 'react-router-dom'
       import { useAuthStore } from '../store/authStore'
       import { exchangeDeeplink } from '../api/auth'

       export function AuthPage() {
         const { uuid } = useParams<{ uuid: string }>()
         const navigate = useNavigate()
         const setAuth = useAuthStore((s) => s.setAuth)
         const [error, setError] = useState<string | null>(null)
         const [loading, setLoading] = useState(true)

         useEffect(() => {
           if (!uuid) {
             setError('Неверная ссылка.')
             setLoading(false)
             return
           }
           exchangeDeeplink(uuid)
             .then(({ access, refresh, client_profile_id, name }) => {
               setAuth({ id: client_profile_id, name }, access, refresh)
               navigate('/cabinet', { replace: true })
             })
             .catch(() => {
               setError('Ссылка истекла или недействительна. Запросите новую в боте.')
               setLoading(false)
             })
         }, [uuid, setAuth, navigate])

         return (
           <div className="min-h-screen flex items-center justify-center bg-slate-50">
             <div className="bg-white rounded-2xl shadow-lg p-8 max-w-md w-full mx-4 text-center">
               {loading && !error && (
                 <>
                   <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-slate-900 mx-auto mb-4" />
                   <p className="text-slate-600 text-lg">Авторизация...</p>
                 </>
               )}
               {error && (
                 <>
                   <div className="text-red-500 text-5xl mb-4">!</div>
                   <p className="text-slate-800 text-lg mb-4">{error}</p>
                   <a href="https://t.me/baqsy_bot" className="text-amber-600 hover:text-amber-700 underline">
                     Перейти в бот
                   </a>
                 </>
               )}
             </div>
           </div>
         )
       }
       ```
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project/frontend" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/api/auth.ts contains `exchangeDeeplink`
    - frontend/src/api/auth.ts contains `deeplink/exchange`
    - frontend/src/api/auth.ts does NOT import from `./axios` (uses plain axios)
    - frontend/src/api/submissions.ts contains `getMySubmission`
    - frontend/src/api/submissions.ts contains `submissions/my`
    - frontend/src/hooks/useSubmission.ts contains `useQuery`
    - frontend/src/hooks/useSubmission.ts contains `enabled: isAuthenticated`
    - frontend/src/pages/AuthPage.tsx contains `exchangeDeeplink`
    - frontend/src/pages/AuthPage.tsx contains `setAuth`
    - frontend/src/pages/AuthPage.tsx contains `navigate('/cabinet'`
    - frontend/src/pages/AuthPage.tsx contains `Ссылка истекла`
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>AuthPage обменивает UUID на JWT через /api/v1/bot/deeplink/exchange/, сохраняет JWT в Zustand/localStorage, редиректит в /cabinet. При ошибке показывает сообщение с ссылкой на бот. useSubmission hook загружает submission авторизованного клиента.</done>
</task>

<task type="auto">
  <name>Task 2: Личный кабинет — StatusProgress, PdfDownloadButton, UpsellCard, CabinetPage</name>
  <files>
    frontend/src/pages/CabinetPage.tsx,
    frontend/src/components/cabinet/StatusProgress.tsx,
    frontend/src/components/cabinet/PdfDownloadButton.tsx,
    frontend/src/components/cabinet/UpsellCard.tsx
  </files>
  <read_first>
    frontend/src/hooks/useSubmission.ts,
    frontend/src/api/payments.ts,
    frontend/src/components/payment/openPaymentWidget.ts,
    frontend/src/store/authStore.ts,
    frontend/src/types/api.ts,
    frontend/src/components/ui/Button.tsx,
    frontend/src/components/ui/Card.tsx,
    frontend/src/components/layout/Header.tsx
  </read_first>
  <action>
    1. Создать `frontend/src/components/cabinet/StatusProgress.tsx`:
       - Props: `status: string`
       - Step progress bar с 4 шагами: «Оплачено» → «Анкета» → «На аудите» → «Готово»
       - Маппинг статусов:
         - `paid`, `in_progress_basic` → шаг 1 (Оплачено активен)
         - `in_progress_full` → шаг 2 (Анкета в процессе)
         - `completed`, `under_audit` → шаг 3 (На аудите)
         - `delivered` → шаг 4 (Готово, все зелёные)
       - Визуал: горизонтальная линия с кружками, завершённые шаги — bg-emerald-500, текущий — bg-amber-500 с пульсацией (animate-pulse), будущие — bg-slate-300
       - Responsive: на mobile шаги вертикально (flex-col), на md+ горизонтально (flex-row)
       - Под каждым кружком — название шага

    2. Создать `frontend/src/components/cabinet/PdfDownloadButton.tsx`:
       - Props: `pdfUrl: string | null`
       - Если pdfUrl — кнопка «Скачать отчёт» (Button variant="primary") с `<a href={pdfUrl} target="_blank" rel="noopener">`
       - Если null — текст «Отчёт ещё не готов» серым цветом (text-slate-400)

    3. Создать `frontend/src/components/cabinet/UpsellCard.tsx`:
       - Props: `submissionId: string, tariffCode: string | null, status: string`
       - Показывается ТОЛЬКО если `tariffCode === 'ashide_1'` И `status` в `['completed', 'under_audit', 'delivered']`
       - Если не показывается — return null
       - Card с highlight=false:
         - Заголовок: «Расширьте до Ashide 2»
         - Текст: «Получите полный анализ по 18-24 параметрам. Доплата 90 000 ₸»
         - Кнопка «Расширить тариф» (Button variant="secondary")
       - При клике кнопки:
         - useState loading=true
         - Вызвать `initiateUpsell(submissionId)` из `api/payments.ts`
         - Передать результат в `openPaymentWidget(config, onSuccess, onFail)`
         - onSuccess: `queryClient.invalidateQueries({ queryKey: ['my-submission'] })` + показать «Тариф обновлён!» (alert или inline message)
         - onFail: alert с причиной
         - loading=false
       - Обработка ошибки API (например, уже ashide_2): показать текст ошибки

    4. Реализовать `frontend/src/pages/CabinetPage.tsx` (заменить заглушку):
       - Использует `useSubmission()` hook и `useAuthStore`
       - Получает `clientProfile` из Zustand (name)
       - Layout:
         - Header (импорт из layout/Header)
         - Контейнер: `max-w-3xl mx-auto px-4 pt-24 pb-12`
         - Заголовок: «Личный кабинет» (h1) + приветствие «Здравствуйте, {name}»
       - Состояния загрузки:
         - `isLoading`: skeleton loader
         - `isError` или нет submission: «Нет активных заказов. Начните с Telegram-бота.» + ссылка на бот
       - Когда submission загружен:
         - Информационная карточка (Card): название компании (из template/industry), тариф, дата создания
         - StatusProgress с status
         - PdfDownloadButton с pdf_url
         - UpsellCard с submission.id, tariff_code, status
       - Кнопка «Выйти» (clearAuth + navigate to /)
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project/frontend" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/cabinet/StatusProgress.tsx contains `paid`
    - frontend/src/components/cabinet/StatusProgress.tsx contains `delivered`
    - frontend/src/components/cabinet/StatusProgress.tsx contains `animate-pulse`
    - frontend/src/components/cabinet/PdfDownloadButton.tsx contains `pdfUrl`
    - frontend/src/components/cabinet/PdfDownloadButton.tsx contains `Скачать отчёт`
    - frontend/src/components/cabinet/UpsellCard.tsx contains `ashide_1`
    - frontend/src/components/cabinet/UpsellCard.tsx contains `initiateUpsell`
    - frontend/src/components/cabinet/UpsellCard.tsx contains `openPaymentWidget`
    - frontend/src/components/cabinet/UpsellCard.tsx contains `90 000`
    - frontend/src/pages/CabinetPage.tsx contains `useSubmission`
    - frontend/src/pages/CabinetPage.tsx contains `useAuthStore`
    - frontend/src/pages/CabinetPage.tsx contains `StatusProgress`
    - frontend/src/pages/CabinetPage.tsx contains `UpsellCard`
    - frontend/src/pages/CabinetPage.tsx contains `PdfDownloadButton`
    - frontend/src/pages/CabinetPage.tsx contains `clearAuth`
    - npm run build exits 0
  </acceptance_criteria>
  <done>Кабинет показывает step progress bar со статусом, кнопку скачивания PDF (когда delivered), кнопку upsell (когда ashide_1). Auth flow полностью работает: /auth/:uuid → JWT → /cabinet. Кнопка выхода очищает auth state.</done>
</task>

</tasks>

<verification>
- `npm run build` проходит без ошибок
- AuthPage вызывает /api/v1/bot/deeplink/exchange/ и сохраняет JWT
- CabinetPage загружает submission через /api/v1/submissions/my/
- UpsellCard вызывает /api/v1/payments/upsell/ и открывает CP Widget
- StatusProgress правильно маппит все статусы submission
</verification>

<success_criteria>
- Deep-link авторизация работает: UUID → JWT → кабинет (WEB-06)
- Кабинет показывает статус заказа с progress bar (WEB-04)
- Кнопка Upsell видна для ashide_1 клиентов и вызывает CP Widget (WEB-05)
- PDF-кнопка показывается при delivered (WEB-04)
- npm run build проходит
</success_criteria>

<output>
After completion, create `.planning/phases/05-react-frontend/05-03-SUMMARY.md`
</output>
