---
phase: 06-pdf-generation-delivery
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - backend/apps/delivery/tasks.py
  - backend/apps/delivery/providers/__init__.py
  - backend/apps/delivery/providers/base.py
  - backend/apps/delivery/providers/wazzup24.py
  - backend/apps/delivery/tests/test_tasks.py
  - backend/apps/delivery/tests/test_providers.py
autonomous: true
requirements:
  - DLV-01
  - DLV-02
  - DLV-03
  - DLV-04
  - DLV-05
  - DLV-06

must_haves:
  truths:
    - "deliver_telegram отправляет сопроводительный текст и PDF через Telegram Bot API sendDocument"
    - "deliver_whatsapp отправляет PDF через Wazzup24Provider с presigned URL в contentUri"
    - "DeliveryLog фиксирует статус queued → delivered для каждого канала"
    - "Временные ошибки (5xx) автоматически повторяются через Celery retry"
    - "После доставки в оба канала Submission.status переходит в delivered через select_for_update"
    - "Если у клиента нет WhatsApp-номера — WA-доставка пропускается с DeliveryLog.status=failed"
  artifacts:
    - path: "backend/apps/delivery/tasks.py"
      provides: "deliver_telegram + deliver_whatsapp Celery tasks + _try_mark_delivered helper"
      exports: ["deliver_telegram", "deliver_whatsapp"]
    - path: "backend/apps/delivery/providers/base.py"
      provides: "WhatsAppProvider ABC"
      exports: ["WhatsAppProvider"]
    - path: "backend/apps/delivery/providers/wazzup24.py"
      provides: "Wazzup24Provider implementation"
      exports: ["Wazzup24Provider"]
    - path: "backend/apps/delivery/tests/test_tasks.py"
      provides: "Tests for delivery tasks"
    - path: "backend/apps/delivery/tests/test_providers.py"
      provides: "Tests for WhatsApp provider"
  key_links:
    - from: "backend/apps/delivery/tasks.py"
      to: "Telegram Bot API"
      via: "requests.post sendMessage + sendDocument"
      pattern: "sendDocument"
    - from: "backend/apps/delivery/tasks.py"
      to: "backend/apps/delivery/providers/wazzup24.py"
      via: "Wazzup24Provider.send_document()"
      pattern: "Wazzup24Provider"
    - from: "backend/apps/delivery/tasks.py"
      to: "backend/apps/submissions/models.py"
      via: "_try_mark_delivered → select_for_update → mark_delivered()"
      pattern: "select_for_update.*mark_delivered"
---

<objective>
Celery-таски доставки PDF в Telegram и WhatsApp с retry, DeliveryLog tracking, WhatsAppProvider абстракцией и FSM transition mark_delivered.

Purpose: Замкнуть pipeline — после генерации PDF (Plan 01) клиент получает отчёт в обоих каналах. Race condition на mark_delivered решается через select_for_update.
Output: Полнофункциональные deliver_telegram/deliver_whatsapp таски, WhatsAppProvider, тесты.
</objective>

<execution_context>
@/Users/a1111/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a1111/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pdf-generation-delivery/06-CONTEXT.md
@.planning/phases/06-pdf-generation-delivery/06-RESEARCH.md
@.planning/phases/06-pdf-generation-delivery/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 — generate_pdf task and models -->

From backend/apps/reports/models.py:
```python
class AuditReport(TimestampedModel):
    class Status(models.TextChoices):
        DRAFT = "draft"
        APPROVED = "approved"
        SENT = "sent"
    submission = models.OneToOneField("submissions.Submission", related_name="report")
    admin_text = models.TextField(blank=True)
    pdf_url = models.URLField(blank=True)
    status = models.CharField(max_length=20, choices=Status.choices, default=Status.DRAFT)
    approved_at = models.DateTimeField(null=True, blank=True)
```

From backend/apps/delivery/models.py:
```python
class DeliveryLog(TimestampedModel):
    class Channel(models.TextChoices):
        TELEGRAM = "telegram"
        WHATSAPP = "whatsapp"
    class Status(models.TextChoices):
        QUEUED = "queued"
        SENT = "sent"
        DELIVERED = "delivered"
        FAILED = "failed"
    report = models.ForeignKey("reports.AuditReport", related_name="deliveries")
    channel = models.CharField(max_length=20)
    status = models.CharField(max_length=20, default=Status.QUEUED)
    external_id = models.CharField(max_length=255, blank=True)
    error = models.TextField(blank=True)
```

From backend/apps/submissions/models.py:
```python
class Submission(UUIDModel, TimestampedModel):
    class Status(models.TextChoices):
        UNDER_AUDIT = "under_audit"
        DELIVERED = "delivered"
    @transition(field=status, source=Status.UNDER_AUDIT, target=Status.DELIVERED)
    def mark_delivered(self): pass
```

From backend/apps/accounts/models.py:
```python
class ClientProfile(TimestampedModel):
    telegram_id = models.BigIntegerField(unique=True)
    name = models.CharField(max_length=255)
    phone_wa = models.CharField(max_length=20, blank=True)  # может быть пустым!
```

From backend/apps/submissions/tasks.py (Telegram pattern):
```python
BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN", "")
# Паттерн: requests.post("https://api.telegram.org/bot{BOT_TOKEN}/sendMessage", json={...}, timeout=10)
```

From backend/baqsy/settings/base.py:
```python
AWS_S3_ENDPOINT_URL = env("MINIO_ENDPOINT_URL", default="http://minio:9000")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WhatsAppProvider ABC + Wazzup24Provider implementation + tests</name>
  <files>
    backend/apps/delivery/providers/__init__.py
    backend/apps/delivery/providers/base.py
    backend/apps/delivery/providers/wazzup24.py
    backend/apps/delivery/tests/test_providers.py
  </files>
  <read_first>
    backend/apps/delivery/models.py
    backend/apps/reports/models.py
    backend/apps/accounts/models.py
    .planning/phases/06-pdf-generation-delivery/06-RESEARCH.md
  </read_first>
  <action>
1. Создать директорию `backend/apps/delivery/providers/`.

2. Создать `backend/apps/delivery/providers/__init__.py`:
   ```python
   from .base import WhatsAppProvider
   from .wazzup24 import Wazzup24Provider

   __all__ = ["WhatsAppProvider", "Wazzup24Provider"]
   ```

3. Создать `backend/apps/delivery/providers/base.py` — абстракция (DLV-03):
   ```python
   from abc import ABC, abstractmethod

   class WhatsAppProvider(ABC):
       @abstractmethod
       def send_document(self, phone: str, file_url: str, caption: str) -> str:
           """Send a document to a WhatsApp number.

           Args:
               phone: WhatsApp phone number (e.g. "77001234567")
               file_url: Public URL to the document (presigned MinIO URL)
               caption: Accompanying text message

           Returns:
               External message ID from the provider
           """
           ...
   ```

4. Создать `backend/apps/delivery/providers/wazzup24.py` — реализация Wazzup24:
   ```python
   import logging
   import requests
   from .base import WhatsAppProvider

   log = logging.getLogger(__name__)

   class Wazzup24Provider(WhatsAppProvider):
       BASE_URL = "https://api.wazzup24.com/v3/message"

       def __init__(self, api_key: str, channel_id: str):
           self.api_key = api_key
           self.channel_id = channel_id

       def send_document(self, phone: str, file_url: str, caption: str) -> str:
           # Нормализовать телефон: убрать "+" если есть
           normalized_phone = phone.lstrip("+")

           resp = requests.post(
               self.BASE_URL,
               headers={
                   "Authorization": f"Bearer {self.api_key}",
                   "Content-Type": "application/json",
               },
               json={
                   "channelId": self.channel_id,
                   "chatId": normalized_phone,
                   "chatType": "whatsapp",
                   "text": caption,
                   "contentUri": file_url,
               },
               timeout=30,
           )
           resp.raise_for_status()
           data = resp.json()
           message_id = str(data.get("messageId", ""))
           log.info("Wazzup24: sent to %s, messageId=%s", normalized_phone, message_id)
           return message_id
   ```

5. Создать `backend/apps/delivery/tests/test_providers.py`:
   - **test_abstract_provider_cannot_instantiate** (DLV-03) — `WhatsAppProvider()` raises `TypeError`
   - **test_wazzup24_send_document_success** — mock `requests.post`, verify request body содержит `channelId`, `chatId`, `chatType=whatsapp`, `contentUri`, `text`; verify returns `messageId`
   - **test_wazzup24_normalizes_phone** — phone `"+77001234567"` → chatId `"77001234567"` в теле запроса
   - **test_wazzup24_raises_on_http_error** — mock `requests.post` returns 500 → raises `requests.HTTPError`
   - **test_wazzup24_auth_header** — verify `Authorization: Bearer {key}` header
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project" && docker-compose exec web pytest apps/delivery/tests/test_providers.py -x -q 2>/dev/null || python -m pytest backend/apps/delivery/tests/test_providers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/apps/delivery/providers/base.py` contains `class WhatsAppProvider(ABC)` with `@abstractmethod def send_document`
    - `backend/apps/delivery/providers/wazzup24.py` contains `class Wazzup24Provider(WhatsAppProvider)` with `BASE_URL = "https://api.wazzup24.com/v3/message"`
    - `backend/apps/delivery/providers/__init__.py` exports both classes
    - `backend/apps/delivery/tests/test_providers.py` contains `test_abstract_provider_cannot_instantiate`, `test_wazzup24_send_document_success`
    - All tests pass
  </acceptance_criteria>
  <done>WhatsAppProvider ABC и Wazzup24Provider реализованы. Нормализация телефона работает. 5 тестов проходят.</done>
</task>

<task type="auto">
  <name>Task 2: deliver_telegram + deliver_whatsapp tasks + _try_mark_delivered + tests</name>
  <files>
    backend/apps/delivery/tasks.py
    backend/apps/delivery/tests/test_tasks.py
  </files>
  <read_first>
    backend/apps/delivery/models.py
    backend/apps/delivery/providers/base.py
    backend/apps/delivery/providers/wazzup24.py
    backend/apps/reports/models.py
    backend/apps/reports/tasks.py
    backend/apps/submissions/models.py
    backend/apps/submissions/tasks.py
    backend/apps/accounts/models.py
  </read_first>
  <action>
1. ПОЛНОСТЬЮ ЗАМЕНИТЬ `backend/apps/delivery/tasks.py` (убрать заглушки из Plan 01):

```python
import logging
import os

import requests
from celery import shared_task
from django.db import transaction
from requests.exceptions import RequestException

log = logging.getLogger(__name__)

BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN", "")


def _try_mark_delivered(report):
    """Check if both channels delivered; if so, transition FSM to delivered.

    Uses select_for_update to prevent race condition between
    deliver_telegram and deliver_whatsapp completing simultaneously.
    """
    from apps.delivery.models import DeliveryLog
    from apps.submissions.models import Submission

    delivered_channels = set(
        DeliveryLog.objects.filter(
            report=report,
            status=DeliveryLog.Status.DELIVERED,
        ).values_list("channel", flat=True)
    )

    # Если у клиента нет WA-номера, достаточно только Telegram
    has_wa = bool(report.submission.client.phone_wa)
    required = {DeliveryLog.Channel.TELEGRAM}
    if has_wa:
        required.add(DeliveryLog.Channel.WHATSAPP)

    if not required.issubset(delivered_channels):
        return

    with transaction.atomic():
        sub = Submission.objects.select_for_update().get(pk=report.submission_id)
        if sub.status == Submission.Status.UNDER_AUDIT:
            sub.mark_delivered()
            sub.save(update_fields=["status"])
            log.info("mark_delivered: submission=%s → delivered", sub.id)


@shared_task(
    name="delivery.deliver_telegram",
    bind=True,
    autoretry_for=(RequestException,),
    max_retries=5,
    retry_backoff=True,
    retry_backoff_max=300,
)
def deliver_telegram(self, report_id: str):
    """Deliver PDF to client via Telegram Bot API sendDocument (DLV-01)."""
    from apps.delivery.models import DeliveryLog
    from apps.reports.models import AuditReport

    report = AuditReport.objects.select_related("submission__client").get(id=report_id)

    # Idempotent: get_or_create prevents duplicates on retry (DLV-04)
    log_entry, _ = DeliveryLog.objects.get_or_create(
        report=report,
        channel=DeliveryLog.Channel.TELEGRAM,
        defaults={"status": DeliveryLog.Status.QUEUED},
    )

    # Skip if already delivered
    if log_entry.status == DeliveryLog.Status.DELIVERED:
        log.info("deliver_telegram: already delivered for report=%s", report_id)
        return

    telegram_id = report.submission.client.telegram_id

    # 1. Сопроводительный текст (DLV-06)
    requests.post(
        f"https://api.telegram.org/bot{BOT_TOKEN}/sendMessage",
        json={
            "chat_id": telegram_id,
            "text": "Спасибо за обращение! Ваш аудит-отчёт готов. 📄",
        },
        timeout=10,
    ).raise_for_status()

    # 2. Скачать PDF и отправить как файл (DLV-01)
    pdf_resp = requests.get(report.pdf_url, timeout=30)
    pdf_resp.raise_for_status()
    pdf_bytes = pdf_resp.content

    doc_resp = requests.post(
        f"https://api.telegram.org/bot{BOT_TOKEN}/sendDocument",
        data={"chat_id": telegram_id},
        files={"document": ("audit_report.pdf", pdf_bytes, "application/pdf")},
        timeout=30,
    )
    doc_resp.raise_for_status()
    message_id = doc_resp.json()["result"]["message_id"]

    # Update DeliveryLog (DLV-04)
    log_entry.status = DeliveryLog.Status.DELIVERED
    log_entry.external_id = str(message_id)
    log_entry.save(update_fields=["status", "external_id"])

    log.info("deliver_telegram: done for report=%s, message_id=%s", report_id, message_id)
    _try_mark_delivered(report)


@shared_task(
    name="delivery.deliver_whatsapp",
    bind=True,
    autoretry_for=(RequestException,),
    max_retries=5,
    retry_backoff=True,
    retry_backoff_max=300,
)
def deliver_whatsapp(self, report_id: str):
    """Deliver PDF to client via WhatsApp through Wazzup24 (DLV-02)."""
    from apps.delivery.models import DeliveryLog
    from apps.reports.models import AuditReport

    report = AuditReport.objects.select_related("submission__client").get(id=report_id)
    client = report.submission.client

    # Edge case: клиент без WhatsApp-номера — skip delivery
    if not client.phone_wa:
        log_entry, _ = DeliveryLog.objects.get_or_create(
            report=report,
            channel=DeliveryLog.Channel.WHATSAPP,
            defaults={
                "status": DeliveryLog.Status.FAILED,
                "error": "no_phone_wa",
            },
        )
        log.warning("deliver_whatsapp: no phone_wa for report=%s, skipping", report_id)
        _try_mark_delivered(report)
        return

    # Idempotent: get_or_create (DLV-04)
    log_entry, _ = DeliveryLog.objects.get_or_create(
        report=report,
        channel=DeliveryLog.Channel.WHATSAPP,
        defaults={"status": DeliveryLog.Status.QUEUED},
    )

    if log_entry.status == DeliveryLog.Status.DELIVERED:
        log.info("deliver_whatsapp: already delivered for report=%s", report_id)
        return

    # Send via Wazzup24Provider (DLV-02, DLV-03)
    from apps.delivery.providers.wazzup24 import Wazzup24Provider

    provider = Wazzup24Provider(
        api_key=os.environ.get("WAZZUP24_API_KEY", ""),
        channel_id=os.environ.get("WAZZUP24_CHANNEL_ID", ""),
    )
    caption = "Спасибо за обращение! Ваш аудит-отчёт готов."
    message_id = provider.send_document(
        phone=client.phone_wa,
        file_url=report.pdf_url,
        caption=caption,
    )

    # Update DeliveryLog (DLV-04)
    log_entry.status = DeliveryLog.Status.DELIVERED
    log_entry.external_id = message_id
    log_entry.save(update_fields=["status", "external_id"])

    log.info("deliver_whatsapp: done for report=%s, messageId=%s", report_id, message_id)
    _try_mark_delivered(report)
```

2. Создать `backend/apps/delivery/tests/test_tasks.py`:
   - Импортировать/создать фабрики: AuditReportFactory (from Plan 01), SubmissionFactory, ClientProfileFactory, TariffFactory
   - Все внешние HTTP-вызовы мокировать через `unittest.mock.patch("requests.post")`, `unittest.mock.patch("requests.get")`

   Тесты:
   - **test_deliver_telegram_sends_text_first** (DLV-06) — mock requests.post, вызвать `deliver_telegram(report_id)`, проверить что первый вызов requests.post содержит `sendMessage` с текстом "Спасибо за обращение"
   - **test_deliver_telegram_sends_document** (DLV-01) — mock requests.post + requests.get (PDF download), проверить вызов `sendDocument` с `files` содержащим `"audit_report.pdf"`
   - **test_deliver_telegram_creates_delivery_log** (DLV-04) — после вызова, `DeliveryLog.objects.get(report=report, channel="telegram")` имеет `status="delivered"` и `external_id` установлен
   - **test_deliver_telegram_idempotent** — DeliveryLog со status=delivered уже существует → requests.post НЕ вызывается
   - **test_deliver_whatsapp_calls_provider** (DLV-02) — mock `Wazzup24Provider.send_document`, проверить вызов с phone=client.phone_wa, file_url=report.pdf_url
   - **test_deliver_whatsapp_creates_delivery_log** (DLV-04) — DeliveryLog с channel=whatsapp, status=delivered после успеха
   - **test_deliver_whatsapp_no_phone_skips** — client.phone_wa="" → DeliveryLog создаётся со status=failed, error="no_phone_wa", Wazzup24Provider НЕ вызывается
   - **test_try_mark_delivered_both_channels** — оба DeliveryLog delivered → submission.status переходит в "delivered"
   - **test_try_mark_delivered_one_missing** — только telegram delivered → submission.status остаётся under_audit
   - **test_try_mark_delivered_no_wa_phone** — client.phone_wa="" + telegram delivered → submission.status="delivered" (WA не требуется)
   - **test_deliver_telegram_retries_on_network_error** (DLV-05) — задача имеет `autoretry_for=(RequestException,)` в атрибутах

   Все тесты используют `@pytest.mark.django_db` и мокируют внешние вызовы.
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project" && docker-compose exec web pytest apps/delivery/tests/test_tasks.py apps/delivery/tests/test_providers.py -x -q 2>/dev/null || python -m pytest backend/apps/delivery/tests/test_tasks.py backend/apps/delivery/tests/test_providers.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/apps/delivery/tasks.py` contains `def deliver_telegram` with `name="delivery.deliver_telegram"` and `autoretry_for=(RequestException,)`
    - `backend/apps/delivery/tasks.py` contains `def deliver_whatsapp` with `name="delivery.deliver_whatsapp"`
    - `backend/apps/delivery/tasks.py` contains `def _try_mark_delivered` with `select_for_update`
    - `backend/apps/delivery/tests/test_tasks.py` contains `test_deliver_telegram_sends_text_first`, `test_deliver_whatsapp_no_phone_skips`, `test_try_mark_delivered_both_channels`
    - All 16+ tests pass (5 providers + 11 tasks)
  </acceptance_criteria>
  <done>deliver_telegram и deliver_whatsapp полностью реализованы. DeliveryLog tracking works. _try_mark_delivered с select_for_update решает race condition. Edge case без WhatsApp корректно обработан. 11+ тестов проходят.</done>
</task>

</tasks>

<verification>
```bash
# Run all phase 6 tests
docker-compose exec web pytest apps/reports/ apps/delivery/ -x -q

# Full test suite — no regressions
docker-compose exec web pytest -x -q

# Verify delivery pipeline end-to-end structure
grep -r "generate_pdf\|deliver_telegram\|deliver_whatsapp" backend/apps/reports/ backend/apps/delivery/ --include="*.py" -l

# Verify FSM mark_delivered wiring
grep "select_for_update\|mark_delivered" backend/apps/delivery/tasks.py
```
</verification>

<success_criteria>
- deliver_telegram отправляет текст + PDF через Bot API с retry на RequestException
- deliver_whatsapp отправляет через Wazzup24Provider, пропускает если нет phone_wa
- DeliveryLog корректно отслеживает queued → delivered для обоих каналов
- _try_mark_delivered переводит Submission в delivered только когда все каналы доставлены
- Race condition решается через select_for_update
- Все тесты проходят (16+ в providers + tasks)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pdf-generation-delivery/06-02-SUMMARY.md`
</output>
