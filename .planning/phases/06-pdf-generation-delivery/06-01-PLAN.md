---
phase: 06-pdf-generation-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/templates/pdf/audit_report.html
  - backend/templates/pdf/styles.css
  - backend/apps/reports/services.py
  - backend/apps/reports/tasks.py
  - backend/apps/reports/views.py
  - backend/apps/reports/serializers.py
  - backend/apps/reports/urls.py
  - backend/apps/core/api_urls.py
  - backend/apps/reports/tests/test_tasks.py
  - backend/apps/reports/tests/test_views.py
autonomous: true
requirements:
  - PDF-01
  - PDF-02
  - PDF-03
  - PDF-04
  - PDF-05
  - PDF-06
  - PDF-07

must_haves:
  truths:
    - "generate_pdf(report_id) создаёт PDF с именем клиента и компанией на обложке"
    - "PDF для ashide_1 содержит 7-9 параметров, для ashide_2 — все параметры включая расширенные секции"
    - "PDF загружается в MinIO и presigned URL сохраняется в AuditReport.pdf_url"
    - "Повторный вызов generate_pdf для отчёта с pdf_url не генерирует новый PDF"
    - "POST /api/v1/reports/{id}/approve/ запускает Celery-цепочку генерации и доставки"
  artifacts:
    - path: "backend/templates/pdf/audit_report.html"
      provides: "Jinja2 HTML-шаблон с условными секциями для ashide_1/ashide_2"
      contains: "tariff.code == 'ashide_2'"
    - path: "backend/templates/pdf/styles.css"
      provides: "CSS для фирменного стиля PDF"
      contains: "font-family"
    - path: "backend/apps/reports/tasks.py"
      provides: "Celery-таск generate_pdf"
      exports: ["generate_pdf"]
    - path: "backend/apps/reports/views.py"
      provides: "ApproveReportView API endpoint"
      exports: ["ApproveReportView"]
    - path: "backend/apps/reports/urls.py"
      provides: "URL routing для reports"
  key_links:
    - from: "backend/apps/reports/tasks.py"
      to: "backend/templates/pdf/audit_report.html"
      via: "jinja2.Environment(FileSystemLoader(...))"
      pattern: "FileSystemLoader.*templates.*pdf"
    - from: "backend/apps/reports/tasks.py"
      to: "MinIO S3"
      via: "boto3.client.put_object + generate_presigned_url"
      pattern: "s3\\.put_object"
    - from: "backend/apps/reports/views.py"
      to: "backend/apps/reports/tasks.py"
      via: "Celery chain(generate_pdf, group(deliver_telegram, deliver_whatsapp))"
      pattern: "chain.*generate_pdf"
---

<objective>
PDF-шаблон (Jinja2 + WeasyPrint) и Celery-таск генерации PDF с сохранением в MinIO. API-эндпоинт подтверждения аудита, запускающий pipeline генерации и доставки.

Purpose: Создать ядро генерации PDF — шаблон, сервис рендеринга, таск, хранение в MinIO. Это фундамент для доставки в Plan 02.
Output: Рабочий generate_pdf таск, approve API, тесты.
</objective>

<execution_context>
@/Users/a1111/.claude/get-shit-done/workflows/execute-plan.md
@/Users/a1111/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pdf-generation-delivery/06-CONTEXT.md
@.planning/phases/06-pdf-generation-delivery/06-RESEARCH.md

<interfaces>
<!-- Existing models and patterns the executor needs -->

From backend/apps/reports/models.py:
```python
class AuditReport(TimestampedModel):
    class Status(models.TextChoices):
        DRAFT = "draft", "Черновик"
        APPROVED = "approved", "Утверждён"
        SENT = "sent", "Отправлен"

    submission = models.OneToOneField("submissions.Submission", on_delete=models.CASCADE, related_name="report")
    admin_text = models.TextField(blank=True)
    pdf_url = models.URLField(blank=True)
    status = models.CharField(max_length=20, choices=Status.choices, default=Status.DRAFT)
    approved_at = models.DateTimeField(null=True, blank=True)
```

From backend/apps/submissions/models.py:
```python
class Submission(UUIDModel, TimestampedModel):
    class Status(models.TextChoices):
        COMPLETED = "completed", "Анкета завершена"
        UNDER_AUDIT = "under_audit", "На аудите"
        DELIVERED = "delivered", "Доставлен"

    client = models.ForeignKey("accounts.ClientProfile", ...)
    template = models.ForeignKey("industries.QuestionnaireTemplate", ...)
    tariff = models.ForeignKey("payments.Tariff", ...)

    @transition(field=status, source=Status.COMPLETED, target=Status.UNDER_AUDIT)
    def start_audit(self): pass

    @transition(field=status, source=Status.UNDER_AUDIT, target=Status.DELIVERED)
    def mark_delivered(self): pass
```

From backend/apps/accounts/models.py:
```python
class ClientProfile(TimestampedModel):
    telegram_id = models.BigIntegerField(unique=True)
    name = models.CharField(max_length=255)
    company = models.CharField(max_length=255)
    phone_wa = models.CharField(max_length=20, blank=True)
    city = models.CharField(max_length=100, blank=True)
    industry = models.ForeignKey("industries.Industry", on_delete=models.SET_NULL, ...)
```

From backend/apps/delivery/models.py:
```python
class DeliveryLog(TimestampedModel):
    class Channel(models.TextChoices):
        TELEGRAM = "telegram"
        WHATSAPP = "whatsapp"
    class Status(models.TextChoices):
        QUEUED = "queued"
        SENT = "sent"
        DELIVERED = "delivered"
        FAILED = "failed"
    report = models.ForeignKey("reports.AuditReport", related_name="deliveries")
    channel = models.CharField(max_length=20, choices=Channel.choices)
    status = models.CharField(max_length=20, choices=Status.choices, default=Status.QUEUED)
    external_id = models.CharField(max_length=255, blank=True)
    error = models.TextField(blank=True)
```

From backend/baqsy/settings/base.py:
```python
AWS_ACCESS_KEY_ID = env("MINIO_ACCESS_KEY", default="")
AWS_SECRET_ACCESS_KEY = env("MINIO_SECRET_KEY", default="")
AWS_STORAGE_BUCKET_NAME = env("MINIO_BUCKET", default="baqsy")
AWS_S3_ENDPOINT_URL = env("MINIO_ENDPOINT_URL", default="http://minio:9000")
AWS_QUERYSTRING_EXPIRE = 60 * 60 * 24 * 7  # 7 days
BASE_DIR = Path(__file__).resolve().parent.parent.parent  # backend/
```

From backend/apps/core/api_urls.py:
```python
urlpatterns = [
    path("bot/", include("apps.accounts.bot_urls")),
    path("content/", include("apps.content.urls")),
    path("industries/", include("apps.industries.urls")),
    path("submissions/", include("apps.submissions.urls")),
    path("payments/", include("apps.payments.urls")),
]
```

From backend/apps/submissions/tasks.py (Celery + Telegram pattern to reuse):
```python
BOT_TOKEN = os.environ.get("TELEGRAM_BOT_TOKEN", "")
BOT_NAME = os.environ.get("TELEGRAM_BOT_USERNAME", "BaqsyBot")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Jinja2 PDF template + CSS + generate_pdf Celery task + services.py</name>
  <files>
    backend/templates/pdf/audit_report.html
    backend/templates/pdf/styles.css
    backend/apps/reports/services.py
    backend/apps/reports/tasks.py
    backend/apps/reports/tests/test_tasks.py
  </files>
  <read_first>
    backend/apps/reports/models.py
    backend/apps/submissions/models.py
    backend/apps/accounts/models.py
    backend/apps/delivery/models.py
    backend/apps/submissions/tasks.py
    backend/baqsy/settings/base.py
    backend/baqsy/celery.py
  </read_first>
  <action>
1. Создать директорию `backend/templates/pdf/`.

2. Создать `backend/templates/pdf/styles.css` — CSS для PDF в фирменном стиле:
   - `@page { size: A4; margin: 2cm; }`
   - `body { font-family: "Liberation Sans", "Roboto", sans-serif; color: #1e293b; }` (slate-900)
   - `.cover-page` — полная первая страница с `page-break-after: always;`, центрированный заголовок, имя клиента, компания, отрасль, тариф, дата
   - Акцентный цвет `#f59e0b` (amber-500) для заголовков и разделителей
   - `.audit-section` — основной текст аудита с `line-height: 1.6; margin: 1em 0;`
   - `.answer-item` — карточка вопрос-ответ с лёгкой рамкой `border-left: 3px solid #f59e0b;`
   - `.extended-section` — дополнительные параметры Ashide 2
   - Не использовать Tailwind в PDF — чистый CSS для WeasyPrint

3. Создать `backend/templates/pdf/audit_report.html` — Jinja2 шаблон (НЕ Django template):
   - `<!DOCTYPE html><html lang="ru">` с `<meta charset="UTF-8">`
   - Подключение CSS: `<link rel="stylesheet" href="styles.css">` — WeasyPrint загрузит по относительному пути
   - **Обложка** (`div.cover-page`): `{{ client.name }}`, `{{ client.company }}`, `{{ client.industry.name }}`, `{{ tariff.title }}`, `{{ generated_at.strftime('%d.%m.%Y') }}`
   - **Текст аудита** (`div.audit-section`): `{{ report.admin_text | safe }}` (safe для HTML-разметки от админа)
   - **Секция ответов** (Ashide 1 — до 9 параметров):
     ```jinja2
     {% for answer in answers %}
       <div class="answer-item">
         <strong>{{ answer.question.text }}</strong>
         <p>{{ answer.display_value }}</p>
       </div>
     {% endfor %}
     ```
   - **Расширенная секция** (только Ashide 2):
     ```jinja2
     {% if tariff.code == 'ashide_2' %}
     <div class="extended-section">
       <h2>Расширенный анализ</h2>
       <!-- Все ответы уже включены выше, эта секция — для дополнительного текста аудита -->
     </div>
     {% endif %}
     ```
   - Примечание: В шаблоне НЕ использовать `{% load %}` — это чистый Jinja2, не Django templates.
   - Для отображения answer.value использовать кастомный Jinja2 фильтр `format_answer_value` который извлекает текст/число/выбор из JSONB value dict.

4. Создать `backend/apps/reports/services.py` — PDFGenerationService:
   - Функция `render_pdf(report: AuditReport) -> bytes`:
     - Создать `jinja2.Environment(loader=FileSystemLoader(str(settings.BASE_DIR / "templates" / "pdf")), autoescape=True)`
     - Зарегистрировать кастомный фильтр `format_answer_value(value_dict)` который из `{"text": "foo"}` возвращает `"foo"`, из `{"number": 42}` → `"42"`, из `{"choice": "option"}` → `"option"`, из `{"choices": ["a", "b"]}` → `"a, b"`
     - Загрузить ответы: `report.submission.answers.select_related("question").order_by("question__order")`
     - Контекст шаблона: `report`, `submission`, `client` (report.submission.client), `tariff` (report.submission.tariff), `answers`, `generated_at` (timezone.now())
     - Рендер: `template.render(**context)` → `html_str`
     - WeasyPrint: `HTML(string=html_str, base_url=str(settings.BASE_DIR / "templates" / "pdf")).write_pdf()` → bytes
     - `base_url` нужен для того чтобы WeasyPrint нашёл `styles.css` по относительному пути из HTML
   - Функция `upload_pdf_to_minio(pdf_bytes: bytes, submission_id: str) -> str`:
     - Создать `boto3.client("s3", endpoint_url=settings.AWS_S3_ENDPOINT_URL, aws_access_key_id=settings.AWS_ACCESS_KEY_ID, aws_secret_access_key=settings.AWS_SECRET_ACCESS_KEY)` ВНУТРИ функции (не на уровне модуля — pitfall prefork fork-safety)
     - Key: `f"pdfs/{submission_id}/{int(timezone.now().timestamp())}.pdf"`
     - `s3.put_object(Bucket=settings.AWS_STORAGE_BUCKET_NAME, Key=key, Body=pdf_bytes, ContentType="application/pdf")`
     - `presigned_url = s3.generate_presigned_url("get_object", Params={"Bucket": settings.AWS_STORAGE_BUCKET_NAME, "Key": key}, ExpiresIn=settings.AWS_QUERYSTRING_EXPIRE)`
     - Return `presigned_url`

5. Создать `backend/apps/reports/tasks.py` — Celery-таск:
   ```python
   import logging
   from celery import shared_task
   from django.utils import timezone

   log = logging.getLogger(__name__)

   @shared_task(
       name="reports.generate_pdf",
       bind=True,
       max_retries=3,
       default_retry_delay=60,
   )
   def generate_pdf(self, report_id: str):
       from apps.reports.models import AuditReport
       from apps.reports.services import render_pdf, upload_pdf_to_minio

       report = AuditReport.objects.select_related(
           "submission__client__industry",
           "submission__tariff",
       ).get(id=report_id)

       # Идемпотентность (PDF-07)
       if report.pdf_url:
           log.info("generate_pdf: already exists for report=%s", report_id)
           return

       pdf_bytes = render_pdf(report)
       presigned_url = upload_pdf_to_minio(pdf_bytes, str(report.submission_id))

       report.pdf_url = presigned_url
       report.save(update_fields=["pdf_url"])
       log.info("generate_pdf: done for report=%s", report_id)
   ```

6. Создать `backend/apps/reports/tests/test_tasks.py`:
   - Использовать `pytest.mark.django_db`, factory-boy фабрики из `apps.*.factories` (уже существуют для Submission, ClientProfile, Industry, Tariff, Question, Answer)
   - Создать фабрику `AuditReportFactory` в тестовом файле или в `apps/reports/factories.py`
   - **test_generate_pdf_creates_pdf** — mock `weasyprint.HTML` и `boto3.client`, вызвать `generate_pdf(str(report.id))`, проверить: `report.refresh_from_db(); assert report.pdf_url`
   - **test_generate_pdf_idempotent** (PDF-07) — установить `report.pdf_url = "http://existing.url"`, вызвать `generate_pdf`, проверить что `boto3.client` НЕ вызывался
   - **test_generate_pdf_ashide1_context** (PDF-01, PDF-04) — mock WeasyPrint, проверить что `HTML(string=...)` получил HTML с именем клиента и компанией
   - **test_generate_pdf_ashide2_extended** (PDF-02) — тариф с code='ashide_2', проверить что HTML содержит 'ashide_2' условную секцию
   - **test_render_pdf_returns_bytes** (PDF-03) — вызвать `render_pdf()` с моком WeasyPrint, проверить возврат bytes
   - **test_format_answer_value_filter** — unit-тест для кастомного фильтра: `{"text": "foo"}` → `"foo"`, `{"number": 42}` → `"42"`, `{"choices": ["a","b"]}` → `"a, b"`
   - Все тесты мокируют `weasyprint.HTML` и `boto3.client` — не запускать реальный WeasyPrint в тестах
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project" && docker-compose exec web pytest apps/reports/tests/test_tasks.py -x -q 2>/dev/null || python -m pytest backend/apps/reports/tests/test_tasks.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/templates/pdf/audit_report.html` exists and contains `{{ client.name }}`, `{{ client.company }}`, `tariff.code == 'ashide_2'`
    - `backend/templates/pdf/styles.css` exists and contains `font-family`, `@page`, `cover-page`
    - `backend/apps/reports/services.py` exports `render_pdf` and `upload_pdf_to_minio`
    - `backend/apps/reports/tasks.py` exports `generate_pdf` shared_task with `name="reports.generate_pdf"`
    - `backend/apps/reports/tests/test_tasks.py` contains `test_generate_pdf_idempotent`, `test_generate_pdf_ashide2_extended`
    - All tests pass
  </acceptance_criteria>
  <done>generate_pdf Celery-таск создаёт PDF через WeasyPrint+Jinja2, загружает в MinIO, сохраняет presigned URL. Идемпотентность работает. 6+ тестов проходят.</done>
</task>

<task type="auto">
  <name>Task 2: ApproveReportView API endpoint + URL wiring + tests</name>
  <files>
    backend/apps/reports/views.py
    backend/apps/reports/serializers.py
    backend/apps/reports/urls.py
    backend/apps/core/api_urls.py
    backend/apps/reports/tests/test_views.py
  </files>
  <read_first>
    backend/apps/reports/models.py
    backend/apps/reports/tasks.py
    backend/apps/submissions/models.py
    backend/apps/core/api_urls.py
    backend/baqsy/settings/base.py
    backend/apps/payments/views.py
  </read_first>
  <action>
1. Создать `backend/apps/reports/serializers.py`:
   ```python
   from rest_framework import serializers
   from apps.reports.models import AuditReport

   class AuditReportSerializer(serializers.ModelSerializer):
       class Meta:
           model = AuditReport
           fields = ["id", "submission", "status", "pdf_url", "approved_at", "created_at"]
           read_only_fields = fields
   ```

2. Создать `backend/apps/reports/views.py` — ApproveReportView:
   - `from rest_framework.views import APIView`
   - `from rest_framework.response import Response`
   - `from rest_framework.permissions import IsAdminUser`
   - `permission_classes = [IsAdminUser]` — только staff через session auth
   - Метод `post(self, request, report_id)`:
     - `report = AuditReport.objects.select_related("submission").get(id=report_id)`
     - Проверить что `report.admin_text` не пустой — если пуст, вернуть 400 `{"error": "admin_text is required before approval"}`
     - FSM: если `sub.status == Submission.Status.COMPLETED` → `sub.start_audit(); sub.save(update_fields=["status"])`
     - Установить `report.approved_at = timezone.now()` если ещё не установлен; `report.save(update_fields=["approved_at"])`
     - Запустить Celery chain:
       ```python
       from celery import chain, group
       from apps.reports.tasks import generate_pdf
       from apps.delivery.tasks import deliver_telegram, deliver_whatsapp
       workflow = chain(
           generate_pdf.s(str(report.id)),
           group(
               deliver_telegram.si(str(report.id)),
               deliver_whatsapp.si(str(report.id)),
           )
       )
       workflow.delay()
       ```
     - Вернуть `Response({"status": "queued"}, status=200)`
     - Обработать `AuditReport.DoesNotExist` → 404
   - ВАЖНО: использовать `.si()` (immutable signature) для deliver_* тасков, потому что `generate_pdf` не возвращает значение нужное deliver_* — они получают `report_id` как аргумент напрямую, а не из результата generate_pdf.
   - ВАЖНО: deliver_telegram и deliver_whatsapp будут созданы в Plan 02. Если их модули ещё не существуют, создать заглушки `backend/apps/delivery/tasks.py` с `@shared_task` стабами которые просто логируют и возвращают (чтобы chain не ломался при тестировании).

3. Создать `backend/apps/delivery/tasks.py` — заглушки для Plan 02:
   ```python
   import logging
   from celery import shared_task

   log = logging.getLogger(__name__)

   @shared_task(name="delivery.deliver_telegram", bind=True, max_retries=5, retry_backoff=True)
   def deliver_telegram(self, report_id: str):
       log.info("deliver_telegram stub: report=%s (will be implemented in plan 02)", report_id)

   @shared_task(name="delivery.deliver_whatsapp", bind=True, max_retries=5, retry_backoff=True)
   def deliver_whatsapp(self, report_id: str):
       log.info("deliver_whatsapp stub: report=%s (will be implemented in plan 02)", report_id)
   ```

4. Создать `backend/apps/reports/urls.py`:
   ```python
   from django.urls import path
   from apps.reports.views import ApproveReportView

   app_name = "reports"

   urlpatterns = [
       path("<int:report_id>/approve/", ApproveReportView.as_view(), name="approve"),
   ]
   ```

5. Обновить `backend/apps/core/api_urls.py` — добавить:
   ```python
   path("reports/", include("apps.reports.urls")),
   ```

6. Создать `backend/apps/reports/tests/test_views.py`:
   - **test_approve_report_requires_admin** — не-staff юзер получает 403
   - **test_approve_report_starts_pipeline** — staff юзер, report с admin_text → 200, проверить что `generate_pdf.s(...)` вызван (mock celery chain)
   - **test_approve_report_transitions_fsm** — submission.status переходит из completed в under_audit
   - **test_approve_report_empty_admin_text** — report без admin_text → 400
   - **test_approve_report_idempotent_fsm** — повторный approve для under_audit submission не ломается
   - Использовать `APIClient` с `client.force_authenticate(user=admin_user)` для session auth
  </action>
  <verify>
    <automated>cd "/Users/a1111/Desktop/projects/oplata project" && docker-compose exec web pytest apps/reports/tests/test_views.py -x -q 2>/dev/null || python -m pytest backend/apps/reports/tests/test_views.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `backend/apps/reports/views.py` contains `class ApproveReportView` with `permission_classes = [IsAdminUser]`
    - `backend/apps/reports/urls.py` contains `path` with `approve`
    - `backend/apps/core/api_urls.py` contains `path("reports/", include("apps.reports.urls"))`
    - `backend/apps/delivery/tasks.py` contains `deliver_telegram` and `deliver_whatsapp` stubs
    - `backend/apps/reports/tests/test_views.py` contains `test_approve_report_requires_admin`, `test_approve_report_starts_pipeline`
    - All tests pass
  </acceptance_criteria>
  <done>ApproveReportView работает: staff POST → FSM transition → Celery chain запускается. Delivery task stubs на месте для Plan 02. 5+ тестов проходят.</done>
</task>

</tasks>

<verification>
```bash
# Run all phase-related tests
docker-compose exec web pytest apps/reports/ -x -q

# Verify key files exist
ls backend/templates/pdf/audit_report.html backend/templates/pdf/styles.css
ls backend/apps/reports/services.py backend/apps/reports/tasks.py backend/apps/reports/views.py

# Verify URL wiring
grep "reports/" backend/apps/core/api_urls.py
```
</verification>

<success_criteria>
- generate_pdf таск создаёт PDF через Jinja2+WeasyPrint и загружает в MinIO
- Идемпотентность: повторный вызов не генерирует новый PDF
- ApproveReportView доступен только staff, запускает Celery chain
- FSM transition completed → under_audit работает
- Все тесты проходят (11+ тестов)
</success_criteria>

<output>
After completion, create `.planning/phases/06-pdf-generation-delivery/06-01-SUMMARY.md`
</output>
